import Image from "next/image";

const Media = () => {
  return (
    <section>
      <div className="container">
        <div className="media">
          <Image src={""} alt={""} />
        </div>
      </div>
    </section>
  );
};

export default Media;
