import Image from "next/image";

const Media = () => {
  // TODO finish module
  return (
    <section>
      <div className="container">
        <div className="media">
          <Image src={""} alt={""} />
        </div>
      </div>
    </section>
  );
};

export default Media;
